import { omniscienceAnalyticsEngine } from './omniscience-analytics-engine';

describe('omniscienceAnalyticsEngine', () => {
  it('should work', () => {
    expect(omniscienceAnalyticsEngine()).toEqual(
      'omniscience-analytics-engine',
    );
  });
});

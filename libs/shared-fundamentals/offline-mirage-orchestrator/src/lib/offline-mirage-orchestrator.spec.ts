import { offlineMirageOrchestrator } from './offline-mirage-orchestrator';

describe('offlineMirageOrchestrator', () => {
  it('should work', () => {
    expect(offlineMirageOrchestrator()).toEqual('offline-mirage-orchestrator');
  });
});

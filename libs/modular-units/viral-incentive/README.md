# viral-incentive-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build viral-incentive-engine` to build the library.

## Running unit tests

Run `nx test viral-incentive-engine` to execute the unit tests via [Jest](https://jestjs.io).

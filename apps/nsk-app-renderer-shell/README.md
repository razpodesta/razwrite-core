/**
 * @apparatus RendererShellManual
 * @role Manual de Operación, Soberanía de Proyección y Gobernanza de la Piel Visual.
 * @status <SEALED_PRODUCTION>
 */

# 🏛️ RENDERER SHELL (EL OJO DE LA MATRIZ)

## 📜 1. DECLARACIÓN DE MISIÓN
El `RendererShell` es el proyector de realidad del ecosistema **RazWrite Core**. Su responsabilidad única es recibir un **Tenant Manifest (ADN)** y transmutarlo en una interfaz de usuario interactiva mediante la técnica de **Server-Driven UI (SDUI)**. Opera como un terminal tonto (Aphasia de Negocio), donde la lógica reside en el núcleo y la visualización es una marioneta de datos.

## 🏗️ 2. ARQUITECTURA DE TRIPLE HILO (M-015)
Este aparato es el punto de encuentro de los tres planos de existencia:
1.  **Surface-Pulse (Hilo Principal):** Renderizado de búnkeres visuales a 60fps constantes.
2.  **Deep-Pulse (Web Worker):** Ejecución de la `SovereignIgniter` para despertar criptografía y telemetría sin bloquear la UI.
3.  **Acid-Pulse (Server):** Entrega del Manifiesto y SEO orgánico mediante React Server Components.

## 🧠 3. PROTOCOLO DE IGNICIÓN (BOOTSTRAP)
El shell no inicia hasta que el `SovereignIgniter` certifique los siguientes hitos:
- [x] **BIOS_DETECTION:** Captura de señales de hardware por el `MetabolicScheduler`.
- [x] **IDENTITY_FORGE:** Generación del Pasaporte Mutante por el `IdentityMutantEngine`.
- [x] **VAULT_UNLOCK:** Apertura de la persistencia cifrada en `IndexedDB`.

## 👮 4. ADUANA DE CALIDAD (GOVERNANCE)
- **Zero Hardcoding Policy:** Prohibido inyectar literales de texto en componentes. Todo contenido fluye desde `componentProperties`.
- **Port Sovereignty:** Anclaje mandatorio al puerto `7860` para compatibilidad con la **Fortaleza Hugging Face**.
- **Atomic Styles:** Tailwind CSS escanea la totalidad de la celosía (`libs/**`) para garantizar consistencia visual dinámica.

Autoridad: Raz Podestá

---

